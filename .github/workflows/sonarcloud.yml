name: SonarCloud Analysis

on:
  push:
    branches: ["main", "master"]
  pull_request:

jobs:
  sonar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Build and analyze
        run: ./mvnw -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=aveave_sonar-cloud-research -Dsonar.organization=sonarcloudresearch
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
